// See ../static/capacitor-bridge.js — that is the live bridge served to the WKWebView.
// The iOS Capacitor app loads its web content from a remote server (see
// capacitor.config.ts → server.url), so the only bridge that ever runs in the
// app is /static/capacitor-bridge.js delivered by FastAPI. Edit that file, not
// this one. This stub exists only so README references to ios_app/capacitor-bridge.js
// don't 404, and to prevent the two files from drifting out of sync again.
