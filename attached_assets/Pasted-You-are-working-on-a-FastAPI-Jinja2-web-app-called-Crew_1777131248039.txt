You are working on a FastAPI/Jinja2 web app called CrewLog. The app has a 
bottom tab bar, off-canvas drawer, and a fixed topbar. It uses Tailwind CSS 
(CDN), HTMX, and a custom design system in `static/cl_design.css`. The app is 
deployed as a PWA with `apple-mobile-web-app-status-bar-style: black-translucent`
and `viewport-fit=cover`.

Perform the following changes exactly to fix the iOS mobile UI to a premium 
standard. Do not change any backend Python files.

─────────────────────────────────────────────────────────────────────────────
TASK 1 — Create `static/ui_ios_prime.css`
─────────────────────────────────────────────────────────────────────────────

Create a new file `static/ui_ios_prime.css` with this exact content:

/* ==========================================================================
   CrewLog — iOS Premium Mobile Overrides
   Loaded LAST, after all other CSS files.
   All rules scoped to max-width: 1023px (mobile + tablet).
   ========================================================================== */

/* ── 1. Topbar: account for Dynamic Island / notch safe-area ── */
.topbar {
    padding-top: env(safe-area-inset-top, 0px);
}
/* Compensate: the inner flex row already has h-12 (48px). Adding safe-area
   padding above it extends the bar correctly without re-sizing the flex row. */
@media (max-width: 1023px) {
    .topbar .container > .flex {
        /* Keep the 48px inner row height; top padding is on the .topbar itself */
        height: 48px;
    }
}

/* ── 2. Hide footer on mobile — tabbar replaces it ── */
@media (max-width: 1023px) {
    .footer-bar { display: none !important; }
}

/* ── 3. Main content container — consistent padding on mobile ── */
@media (max-width: 1023px) {
    main.ui-container,
    main.content-container {
        padding-left: 16px;
        padding-right: 16px;
        padding-top: 20px;
    }
}

/* ── 4. Tab bar active state — pill indicator (iOS-style) ── */
@media (max-width: 1023px) {
    .cl-tabbar__item--active .cl-tabbar__icon {
        background: var(--cl-accent-soft, #e7f0fa);
        border-radius: 10px;
        padding: 4px 14px;
        margin-bottom: 1px;
        transition: background .2s;
    }
    .cl-tabbar__item {
        padding: 6px 2px 4px;
    }
    /* Slightly taller inner bar on mobile for comfortable tap targets */
    .cl-tabbar__inner {
        height: 52px;
    }
}

/* ── 5. Global card upgrades for legacy pages ── */
/* Apply card-like styling to any .ui-card or .card that is NOT already cl-card */
@media (max-width: 1023px) {
    .ui-card:not(.cl-card),
    .card:not(.cl-card) {
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 1px 3px rgba(15,41,66,.06), 0 4px 14px rgba(15,41,66,.05);
        margin-bottom: 12px;
    }
}

/* ── 6. List rows — minimum tap target 44px ── */
@media (max-width: 1023px) {
    /* Expense, deposit, crew list rows */
    .expense-row,
    .deposit-row,
    .crew-row,
    .list-row,
    tr.clickable {
        min-height: 44px;
    }

    /* Table rows with links: make entire row tappable feel */
    .cl-table tbody tr td {
        padding-top: 13px;
        padding-bottom: 13px;
    }

    /* Table wrapper: horizontal scroll with momentum */
    .cl-table-scroll,
    .table-responsive,
    .overflow-x-auto {
        -webkit-overflow-scrolling: touch;
        scroll-snap-type: x proximity;
    }
}

/* ── 7. All form inputs globally — prevent iOS zoom, enforce tap height ── */
input:not([type="checkbox"]):not([type="radio"]):not([type="range"]),
select,
textarea {
    font-size: 16px !important;  /* prevents iOS auto-zoom */
    min-height: 44px;
    border-radius: 8px;
    box-sizing: border-box;
}
textarea {
    min-height: 88px;
}

/* ── 8. Buttons — minimum tap target everywhere ── */
button,
.btn,
a.btn,
[role="button"],
.cl-btn {
    min-height: 44px;
}

/* ── 9. Page-level vertical scroll momentum ── */
html, body {
    -webkit-overflow-scrolling: touch;
}
main {
    -webkit-overflow-scrolling: touch;
}

/* ── 10. Drawer refinements ── */
@media (max-width: 1023px) {
    .nav-drawer {
        /* Wider drawer on larger phones */
        --drawer-width: min(84vw, 320px);
    }
    .drawer-link {
        min-height: 44px;
        display: flex;
        align-items: center;
        padding-top: 10px;
        padding-bottom: 10px;
    }
    .drawer-footer {
        padding-bottom: calc(16px + env(safe-area-inset-bottom, 0px));
    }
}

/* ── 11. Night mode — replace red tones with nautical deep blue ── */
html[data-theme="night"] .cl-tabbar {
    background: rgba(10, 18, 35, .95) !important;
    border-top-color: rgba(100, 140, 200, .2) !important;
}
html[data-theme="night"] .cl-tabbar__item {
    color: rgba(180, 200, 230, .55) !important;
}
html[data-theme="night"] .cl-tabbar__item--active,
html[data-theme="night"] .cl-tabbar__item:hover {
    color: rgba(100, 170, 255, .95) !important;
}
html[data-theme="night"] .cl-tabbar__item--active .cl-tabbar__icon {
    background: rgba(29, 111, 187, .2) !important;
}
html[data-theme="night"] .cl-actionbar {
    background: rgba(10, 18, 35, .92) !important;
    border-top-color: rgba(100, 140, 200, .2) !important;
}
html[data-theme="night"] .topbar {
    background: rgba(8, 15, 28, .98) !important;
}

/* ── 12. Overscroll background colour matches topbar on pull-down ── */
@media (max-width: 1023px) {
    html {
        background-color: #0f2942;  /* matches --cl-navy; shows on rubber-band */
        overscroll-behavior-y: none;
    }
    body {
        background-color: var(--cl-bg, #f6f8fb);
        overscroll-behavior-y: none;
    }
}

/* ── 13. Pill/badge sizing on mobile ── */
@media (max-width: 1023px) {
    .cl-pill {
        font-size: 11px;
        padding: 2px 8px;
    }
}

/* ── 14. Hero/KPI grids — single column on narrow phones ── */
@media (max-width: 374px) {
    .cl-grid--2,
    .cl-grid--3,
    .cl-grid--4 {
        grid-template-columns: 1fr !important;
    }
}

/* ── 15. Flash/alert messages — full width on mobile ── */
@media (max-width: 1023px) {
    .cl-alert {
        border-radius: 10px;
        padding: 12px 14px;
    }
}

─────────────────────────────────────────────────────────────────────────────
TASK 2 — Update `templates/layout.html`
─────────────────────────────────────────────────────────────────────────────

In `templates/layout.html`, make these two changes:

CHANGE A — Add the new CSS file as the very LAST stylesheet in the <head>,
after the existing line:
    <link rel="stylesheet" href="/static/cl_design.css">

Add immediately after it:
    <link rel="stylesheet" href="/static/ui_ios_prime.css">

CHANGE B — Fix the topbar nav element. Find this line (around line 294):
    <nav class="topbar navy-shell sticky top-0 z-50">

Change it to:
    <nav class="topbar navy-shell sticky top-0 z-50" style="padding-top: env(safe-area-inset-top, 0px);">

This ensures the topbar accounts for the iPhone notch/Dynamic Island in PWA
black-translucent mode even before the external CSS loads.

─────────────────────────────────────────────────────────────────────────────
TASK 3 — Update `static/ui_mobile_skin.css`
─────────────────────────────────────────────────────────────────────────────

The file currently wraps all rules inside `.logbook-form-wrapper` and 
`.logbook-detail-wrapper` selectors, limiting them to only logbook pages.

Find the outermost wrapper selectors and remove them so the rules apply 
globally. Specifically:

1. Find and REMOVE any line that reads:
       .logbook-form-wrapper,
       .logbook-detail-wrapper {
   ...and its matching closing brace at the bottom of the file.

2. Keep all the rules inside — just un-nest them so they apply site-wide.

If the file uses a single wrapper block like:
    .logbook-form-wrapper { ... all rules nested inside ... }
then extract all the nested rules to the top level.

─────────────────────────────────────────────────────────────────────────────
TASK 4 — Improve bottom tab bar icon quality in `templates/layout.html`
─────────────────────────────────────────────────────────────────────────────

Replace the emoji-based tab bar icons with clean SVG icons for a native-app 
quality look. Find the <nav class="cl-tabbar"> block (around line 428) and 
replace it entirely with:

    {% if request.session.get('user_id') or request.session.get('saas_user_id') %}
    <nav class="cl-tabbar" aria-label="{{ t('nav.navigation') }}">
        <div class="cl-tabbar__inner">
            <a href="/" class="cl-tabbar__item" data-match="/">
                <span class="cl-tabbar__icon" aria-hidden="true">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
                        <polyline points="9 22 9 12 15 12 15 22"/>
                    </svg>
                </span>
                <span>{{ t('nav.dashboard') }}</span>
            </a>
            <a href="/logbook" class="cl-tabbar__item" data-match="/logbook">
                <span class="cl-tabbar__icon" aria-hidden="true">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/>
                        <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
                    </svg>
                </span>
                <span>{{ t('nav.logbook') }}</span>
            </a>
            <a href="/expenses" class="cl-tabbar__item" data-match="/expenses|/deposits|/balances|/settlement|/templates">
                <span class="cl-tabbar__icon" aria-hidden="true">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
                        <rect x="2" y="5" width="20" height="14" rx="2"/>
                        <line x1="2" y1="10" x2="22" y2="10"/>
                    </svg>
                </span>
                <span>{{ t('nav.finances') }}</span>
            </a>
            <a href="/trips/" class="cl-tabbar__item" data-match="/trips">
                <span class="cl-tabbar__icon" aria-hidden="true">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M22 18H2a4 4 0 0 0 4-4V6l5 4 3-5 3 5 5-4v8a4 4 0 0 0 4 4z"/>
                        <line x1="2" y1="22" x2="22" y2="22"/>
                    </svg>
                </span>
                <span>{{ t('nav.trips') }}</span>
            </a>
            <button type="button" class="cl-tabbar__item" id="tabbar-more" aria-label="{{ t('nav.open_menu') }}">
                <span class="cl-tabbar__icon" aria-hidden="true">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
                        <line x1="3" y1="12" x2="21" y2="12"/>
                        <line x1="3" y1="6" x2="21" y2="6"/>
                        <line x1="3" y1="18" x2="21" y2="18"/>
                    </svg>
                </span>
                <span>{{ t('nav.admin') }}</span>
            </button>
        </div>
    </nav>
    {% endif %}

─────────────────────────────────────────────────────────────────────────────
TASK 5 — Fix `cl_design.css` tab bar active icon padding (SVG fix)
─────────────────────────────────────────────────────────────────────────────

The `.cl-tabbar__icon` font-size: 20px was sized for emoji. For inline SVG 
icons we need the icon to render at the right size. In `static/cl_design.css`,
find:

    .cl-tabbar__icon { font-size: 20px; line-height: 1; opacity: .9; }

And change it to:

    .cl-tabbar__icon { 
        font-size: 20px; 
        line-height: 1; 
        opacity: .9;
        display: flex;
        align-items: center;
        justify-content: center;
        width: 28px;
        height: 24px;
    }
    .cl-tabbar__icon svg {
        width: 22px;
        height: 22px;
        flex-shrink: 0;
    }

─────────────────────────────────────────────────────────────────────────────
TASK 6 — Update `static/ui_v1.css`: topbar safe-area height variable
─────────────────────────────────────────────────────────────────────────────

At the very top of `static/ui_v1.css`, inside the existing `:root { }` block,
add these two custom properties if they don't already exist:

    --topbar-safe-h: calc(48px + env(safe-area-inset-top, 0px));
    --topbar-h: 48px;

This allows any template that needs to offset content below the topbar to use
`var(--topbar-safe-h)` for correct positioning on Dynamic Island devices.

─────────────────────────────────────────────────────────────────────────────
VERIFICATION CHECKLIST (do not skip)
─────────────────────────────────────────────────────────────────────────────

After making all changes, confirm:

1. `static/ui_ios_prime.css` exists and is non-empty
2. `templates/layout.html` loads `ui_ios_prime.css` as the LAST stylesheet
3. The topbar <nav> element has `padding-top: env(safe-area-inset-top, 0px)`
4. The .cl-tabbar block in layout.html uses <svg> icons not emoji
5. `static/cl_design.css` has `.cl-tabbar__icon` with display:flex and SVG sizing
6. `static/ui_mobile_skin.css` rules are NOT wrapped in .logbook-form-wrapper scope
7. No Python/backend files were modified
8. Run: python -m pytest test_regression_logbook.py -v   — all tests must pass